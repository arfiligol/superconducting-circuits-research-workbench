sc_data_contracts
=================

``sc_data_contracts`` is the shared Python package reserved for task, manifest,
and trace contract schemas that need an independent package boundary.

.. automodule:: sc_data_contracts
