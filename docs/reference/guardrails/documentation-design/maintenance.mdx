---
aliases:
  - "Documentation Maintenance"
  - "文件維護"
tags:
  - diataxis/reference
  - audience/team
  - sot/true
  - topic/documentation
status: stable
owner: docs-team
audience: team
scope: "文件維護規範：Astro + Starlight docs source/API docs source/暫存樹邊界、版本與 Frontmatter 更新、build 檢查"
version: v2.1.0
last_updated: 2026-06-12
updated_by: codex
---

import { Aside, TabItem, Tabs } from '@astrojs/starlight/components';

# Documentation Maintenance

本文件定義文件維護流程。`docs/` 是 high-level technical docs 的唯一編輯來源；Astro + Starlight 負責把它建成 `/docs/`；Sphinx 與 Documenter.jl 負責 sibling API reference sites。

## Source Tree Map

| Location | Role |
| --- | --- |
| `docs/start/` | shortest onboarding path |
| `docs/workflows/` | task-first workflow docs |
| `docs/concepts/` | mental models, physics concepts, architecture concepts, design decisions |
| `docs/reference/` | stable contracts, API landing, guardrails, agent skills, contributors |
| `docs/contribute/` | contribution workflow entrypoints |
| `docs/api-reference/` | Sphinx and Documenter.jl API docs source |
| `site/src/content/docs/docs/` | generated Starlight staging tree |
| `site/dist/docs/` | built Starlight docs output |
| `build/api-reference/` | generated API docs output before public artifact copy |
| `site/dist/api/` | deployed API docs artifact path |

<Aside type="caution" title="維護規則">

- 日常編輯一律在 `docs/` 進行。
- `docs/start/`、`docs/workflows/`、`docs/concepts/`、`docs/reference/`、`docs/contribute/` 是 active Starlight source layout。
- 禁止直接修改 `site/src/content/docs/docs/`，因為它會由 `scripts/build/prepare_starlight_docs.py` 覆蓋。
- API reference source 放在 `docs/api-reference/`，不得放進 Starlight staging。
- 新增、刪除或移動頁面後，必須跑 route validation 和 Starlight build。

</Aside>

## Frontmatter 更新

| Change type | Minimum update |
| --- | --- |
| content changed | `last_updated`, `updated_by` |
| rule/page scope changed | `version` minor or major bump |
| typo / formatting only | patch bump |

內容有改動時，至少更新：

- `last_updated`: `YYYY-MM-DD`
- `updated_by`: `team` 或 `team/person`

## Astro + Starlight zh-TW Build

本專案目前的正式文件架構名稱為：

- `Astro + Starlight zh-TW Build`

其技術特徵為：

- 主來源樹為 `docs/`
- Astro + Starlight 在 `site/` 內建置 `/docs/`
- `scripts/build/prepare_starlight_docs.py` 產生 Starlight staging
- generated API reference 以 sibling sites 部署到 `/api/python/` 與 `/api/julia/`
- public artifact 由 `site/dist/` 發布

## 檢查流程

<Tabs>
<TabItem label="本地預覽">


```bash
uv run docs-site serve --build
```

</TabItem>

<TabItem label="建置檢查">


```bash
uv run python scripts/check_docs_nav_routes.py --check-source
./scripts/build_docs_sites.sh
uv run python scripts/check_docs_nav_routes.py --check-built

# API reference
./scripts/build/build_api_docs.sh
```

</TabItem>

</Tabs>

<Aside type="tip" title="常見問題">

- 若頁面更新後仍 404，先確認 `./scripts/build_docs_sites.sh` 是否重新產生 Starlight staging。
- 若 Starlight build 失敗，先看 `site/src/content/docs/docs/` 中對應 staging file，再回 `docs/` 修 source。
- 若 API reference 連結失效，跑完整 `./scripts/build/build_public_site.sh`，確認 `/docs/`、`/api/python/`、`/api/julia/` 同時存在。

</Aside>

---

## Agent Rule
```markdown
## Documentation Maintenance
- **Docs Source**: edit `docs/` only.
- **Docs IA**: keep editable docs in the reader/task-first source layout: `docs/start/`, `docs/workflows/`, `docs/concepts/`, `docs/reference/`, and `docs/contribute/`.
- **Staging Tree**: `site/src/content/docs/docs/` is generated by `scripts/build/prepare_starlight_docs.py`; do not edit directly.
- **API Docs Source**: edit Sphinx and Documenter.jl API docs under `docs/api-reference/`; generated output goes through `build/api-reference/` and `site/dist/api/`.
- **Build Owner**: Astro + Starlight renders `/docs/`; Sphinx renders `/api/python/`; Documenter.jl renders `/api/julia/`.
- **Frontmatter**: update `last_updated` and `updated_by` on content changes.
- **Versioning**: patch/minor/major bumps for doc changes.
- **Architecture Term**: this repo uses `Astro + Starlight zh-TW Build`.
- **Verify**: run `uv run python scripts/check_docs_nav_routes.py --check-source`, `./scripts/build_docs_sites.sh`, `uv run python scripts/check_docs_nav_routes.py --check-built`, and `./scripts/build/build_api_docs.sh` when API reference is touched.
```
