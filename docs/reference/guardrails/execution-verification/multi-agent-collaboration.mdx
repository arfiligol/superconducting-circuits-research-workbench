---
aliases:
 - "Codex Subagent Coordination"
 - "Subagent Work Lanes"
 - "Multi-Agent collaboration"
tags:
 - audience/team
 - sot/true
status: stable
owner: docs-team
audience: team
scope: Bounded internal-worker coordination after repository and path ownership are resolved by the active host authority.
version: v5.0.0
last_updated: 2026-08-21
updated_by: codex
---

import { Aside } from '@astrojs/starlight/components';

# Codex Internal-Worker Coordination

Internal workers are implementation mechanisms, not collaboration authorities.
Resolve the repository owner, semantic authority, data boundary, delivery
profile, and exact paths before assigning any worker.

<Aside type="caution" title="Host ownership comes first">

Under SCQ_Design, load `$scq-collaboration-roles` and resolve the registered
owner before considering delegation. Load `$model-routing` only after that
boundary is known. This page does not define repository roles or GitFlow.

</Aside>

## Owner Responsibilities

The resolved repository owner:

- holds the repository write queue and assigns one primary writer per path;
- gives each worker an exact bounded package, allowed paths, validation, and
  stop conditions;
- sequences overlapping work instead of allowing concurrent writers;
- reconciles worker results into the current repository package; and
- retains Human communication, semantic escalation, publication, merge, and
  final-report responsibilities assigned by the host authority.

## Worker Boundaries

An internal worker may inspect or change only its assigned paths. It must not:

- claim Human acceptance or create a Gate;
- change goal semantics, public contracts, data ownership, or repository
  ownership;
- negotiate directly with another repository owner;
- merge protected branches, publish outside the assigned package, or clean
  shared state; or
- create persistent coordination files merely to relay its work.

When its package no longer fits the assigned boundary, the worker stops and
returns the exact conflict to the repository owner.

## Handoff

Return only the changed paths or artifact identity, validation, blockers, and
residual risks needed by the repository owner. The owner synthesizes the
repository result and follows the host-selected delivery profile.

## Related

- [Branch & Worktree Flow](branch-and-worktree-flow.mdx)
- [Task Scope Sizing](./prompt-grading.md)
- [Work Summary Formats](./contributor-reporting.md)
- [Testing](testing.mdx)

## Agent Rule
```markdown
## Codex Internal-Worker Coordination
- Resolve host semantic authority, repository ownership, data boundary, delivery profile, and exact paths before delegation.
- Under SCQ_Design, load `$scq-collaboration-roles`; load `$model-routing` only after ownership is resolved.
- The resolved repository owner holds path leases and assigns one primary writer per path.
- Internal workers receive bounded implementation packages; they do not gain repository, semantic, Human-acceptance, publication, merge, synchronization, or cleanup authority.
- Workers must not negotiate cross-owner changes or redefine public contracts, data ownership, or goal semantics.
- Sequence overlapping paths; do not allow concurrent writers on the same path.
- Workers return changed paths or artifact identity, validation, blockers, and residual risks to the repository owner.
- This rule does not define GitFlow; use the host-selected delivery profile.
```
