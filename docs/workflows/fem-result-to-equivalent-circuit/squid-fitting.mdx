---
aliases:
 - "Fitting SQUID Parameters"
 - "SQUID parameter fitting"
tags:
 - diataxis/how-to
 - audience/user
 - sot/true
 - topic/analysis
status: stable
owner: team
audience: user
scope: "How to fit the current ideal two-junction LC surrogate parameters (Ls, C) from resonance data"
version: v1.2.0
last_updated: 2026-07-10
updated_by: codex
sidebar:
 label: SQUID Fitting
 order: 30
---

import { Aside } from '@astrojs/starlight/components';

# Fitting the Current Two-Junction LC Surrogate

This guide describes the execution boundary of the current ideal symmetric
two-junction LC surrogate. Reusable fitting belongs to Python Analysis Core;
exploratory setup belongs in Pluto or Python notebooks.

## Canonical Knowledge

- [Josephson Current, Phase, Energy, and Inductance](https://github.com/arfiligol/SCQ_Design/blob/main/docs/knowledge/josephson-physics/josephson-current-phase-energy-and-inductance.qmd)
- [DC-SQUID Flux Tunability](https://github.com/arfiligol/SCQ_Design/blob/main/docs/knowledge/josephson-physics/dc-squid-flux-tunability.qmd)

The current Python model accepts `L_jun` as the small-signal inductance of each
of two identical junctions and uses `L_sq = L_jun / 2` before adding `Ls_nH`.
It does not compute `L_jun` from flux, critical current, junction asymmetry,
bias phase, or loop self-inductance. A full flux-tunable DC-SQUID model is not
implemented.

<Aside type="note" title="Preconditions">

- HFSS admittance or notebook-produced trace data has been prepared.
- The frequency axis, unit, port definition and target resonance interval have been confirmed.

</Aside>

---

## Workflow

```mermaid
flowchart LR
  HFSS["HFSS Export"] --> Files["Local Research Files"]
  Files --> Inspect["Notebook Inspection"]
  Inspect --> Fit["Python Analysis Core Fit"]
  Fit --> Result["Figures + Derived Parameters"]
```

## Prepare HFSS Data

Export only the traces needed by the fitting study:

- admittance files for Im(Y) resonance extraction;
- scattering files for S-parameter phase or magnitude inspection;
- enough frequency range to cover the expected modes.

Keep exported files in a local import directory, for example:

```text
data/import/hfss/LJPAL658_v1/
```

## Inspect Data

Load the files in a Pluto or Python notebook. Keep source paths, units, and axis conventions next to the analysis cells.

Verify:

- trace family;
- axes and units;
- frequency span and resonance coverage;
- representation: real/imaginary, magnitude/phase, or complex values.

Move to fitting only after the trace family, port definition, and target resonance window are explicit.

---

## Select Fitting Strategy

Choose the fitting strategy that matches the reviewed fixed parameters:

| Strategy | Applicable scenario | Python Analysis Core entry point |
|------|----------|------------|
| Fit $L_s$ and $C$ | Both parameters are unknown | `fit_squid_model_with_Ls` |
| Fix $C$, fit $L_s$ | A reviewed capacitance is available | `fit_squid_model_with_Ls_fixed_C` |
| Fix $L_s = 0$, fit $C$ | The series inductance is intentionally omitted | `fit_squid_model` |

These functions live in
`superconducting_circuits_analysis.application.analysis.fitting.modes`. They
fit resonance frequency versus supplied per-junction `L_jun`; they do not fit
external flux.

---

## Operation steps

Use a Pluto notebook when you need direct exploration or model iteration.
The notebook may call Julia Core directly and inspect intermediate arrays.

Use a Python notebook when the input is already in Python-native arrays, CSV exports, or tables.
Move shared model fitting code into Python Analysis Core when it is used by more than one notebook.

<Aside type="note" title="Fitting execution boundary">

The ideal two-junction LC surrogate fit belongs to either direct research execution in Pluto or reusable Python Analysis Core code.
Keep notebook-only glue out of package code; keep reusable fitting functions covered by package tests.

</Aside>

---

## Record Results

After the fitting is completed, the study results should be recorded:

1. source trace path or notebook cell provenance
2. fitting entry point and fixed parameters
3. fitted values and uncertainty/error metrics
4. plots or tables used in the analysis report
5. figure generation code and table-generation code needed to reproduce the report evidence
6. notes on whether the fitting logic stayed notebook-local or was promoted into Python Analysis Core

---

## Related references

- [Research Evidence](research-data-evidence.mdx)
- [Notebook Interface](../../reference/notebooks/index.md)
- [Python Core](../../reference/core/python-core.mdx)
- [Flux Dependence Analysis](flux-analysis.md)
